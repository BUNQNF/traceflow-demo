# TraceFlow T6 验收记录

## 2026-08-01 主交付链路

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 项目入口页 | 通过 | 首屏显示“从已创建的项目继续”和“创建项目”；展开后可从原生选择框选择头像工坊，两个入口在 `720px` 高度内完整可见且无横向溢出。 |
| 打开已有项目 | 通过 | 打开头像工坊后直接进入项目总览，显示 `已交付 · v1.0`、`100%` 进度、9 条需求和已开放的变更协同。 |
| 项目创建 | 通过 | “创建项目”进入六项全空表单；支持上传需求文档或直接填写，内置头像工坊示例仍可按需载入。 |
| 项目总览阶段与操作 | 通过 | 五阶段进度条以绿色背景强调当前阶段；进行中阶段的主操作不占用项目标题区，而是位于对应视图卡内：需求视图进入分析，流程原型视图继续原型，视觉视图继续 UI，运行视图继续前端或验收。 |
| 示例运营目标 | 通过 | 头像工坊示例显示新增下载量 `600+`、日均使用时长增加 `3-5 分钟`、社群新增 `200 人` 三项数据目标。 |
| 需求分析与确认 | 通过 | AI 生成与需求中心一致的完整 `REQ-001` 至 `REQ-009` 对照表；需求内容与优先级可编辑，可新增自动编号需求并删除非唯一行；四类下游资产只读显示“待创建”，不显示内部编号，保存草稿后刷新仍保留。提交关联角色审核后，在屏幕正中显示“需求方已自动审核通过（演示）”浮窗并轻度模糊压暗背景，再建立 `v1.0` 并解锁原型。 |
| 需求对照表 | 通过 | AI 分析后生成 `REQ-001` 至 `REQ-009`；人工确认后冻结为 `v1.0`。表头使用“流程依据”“视觉依据”；原型生成后，流程依据具体显示 `prototype.md` 中一个或多个 `AVATAR-*` 页面、加载或异常状态 ID，并随确认从“待审核”更新为“已确认”。 |
| 原型生成与交接 | 通过 | 上游需求可一键复制为 Prompt；本阶段可上传补充交付物；AI 生成以 `prototype.md` 为主要产出的三项文件，其中页面与状态表包含唯一状态 ID、进入条件、动作、反馈、数据变化和离开条件；页面下方按需求展示 `REQ-* -> AVATAR-*` 更新。按钮为“提交关联角色审核”，依次显示需求方与开发自动审核通过提示后，仅解锁 UI 阶段。 |
| UI 设计与双路线审核 | 通过 | 上游输入可一键复制需求为关键帧 Prompt，内容包含 `REQ-*` 与 `prototype.md` 状态 ID；生成 `D-01` 至 `D-03` 后同时显示路线 A“通过关键帧审核 → 制作高保真原型”和路线 B“通过全部 UI 审核 → 制作实际页面”。 |
| 前端开发与审核 | 通过 | 路线 A 会让总览与实现页显示“制作高保真可交互原型”及对应三项交付物；路线 B 显示“制作实际 H5 页面”及实际页面交付物。两条路线分别写入审计，完成实现审核后仅解锁验收阶段。 |
| 验收交付 | 通过 | 五项必验清单全部通过后才能确认交付；项目与四视图同步为 `v1.0 已交付`。 |
| 任意阶段发起变更 | 通过 | 项目总览在需求确认、原型、UI、前端、验收和已交付阶段始终提供“发起需求变更”；变更协同导航不再受交付阶段限制，固定的业务、产品、UI、前端或验收角色均可作为发起人。 |
| AI 变更影响分析 | 通过 | 输入“新增主题模板自定义色彩变体”后，AI 判断为功能增强并从需求对照表命中 `REQ-003`，沿 `prototype.md` 状态、视觉依据、`FE-03` 和 `AC-03` 生成覆盖四视图的五项责任任务；修改输入会使旧分析失效。 |
| 变更申请与角色流转 | 通过 | 提交 `CR-001` 后，五项任务先显示“待同意”；产品经理、UI 设计师、前端工程师与验收方必须分别同意后，才可填写或载入演示变更内容、上传附件并提交。全部提交后显示 `v1.1` 内容齐备，并在同页保留申请、影响依据、任务状态和完整审计。 |
| 方案资产 | 通过 | 各视图随阶段逐步出现；资料来源、版本、责任角色、关联对象和模拟边界可见；预览弹层可关闭。 |
| 刷新恢复与审计 | 通过 | 刷新后恢复当前阶段；每次生成、审核和交付保留本地审计记录。 |
| 重置演示 | 通过 | 产品内二次确认后清除当前状态并返回项目选择/新建页。 |
| 响应式 | 通过 | 桌面主流程与 `375 x 812` 空状态、创建表单、总览通过截图检查；变更协同在 `375 x 812` 竖屏和 `812 x 375` 横屏下，五步进度、长资产编号和五项任务均无横向溢出。 |
| 构建与运行错误 | 通过 | `npm run build` 通过；浏览器控制台错误记录为空。 |

## 当前未完成

- `v1.1` 正式联合验收、发布版本差异与回退仍需继续实现；本轮已完成变更内容齐备前的全部协同流转。
- AI 生成失败、审核驳回和验收失败的可恢复分支仍需补齐。
